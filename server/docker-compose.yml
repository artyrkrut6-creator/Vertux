version: '3.8'
services:
  remote-proxy:
    build: .
    container_name: remote-llm-proxy
    environment:
      - PORT=9000
      - NODE_ENV=production
    ports:
      - "9000:9000"
    restart: unless-stopped
